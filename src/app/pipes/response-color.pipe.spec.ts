import { ResponseColorPipe } from './response-color.pipe';

describe('ResponseColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponseColorPipe();
    expect(pipe).toBeTruthy();
  });
});
