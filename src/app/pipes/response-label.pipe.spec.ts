import { ResponseLabelPipe } from './response-label.pipe';

describe('ResponseLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponseLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
